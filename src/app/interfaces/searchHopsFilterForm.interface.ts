import { FormControl } from '@angular/forms';
export interface ISearchHopsFilterForm {
  chinook: FormControl<boolean>;
  amarillo: FormControl<boolean>;
  simcoe: FormControl<boolean>;
  citra: FormControl<boolean>;
  mosaic: FormControl<boolean>;
  columbus: FormControl<boolean>;
  centennial: FormControl<boolean>;
  comet: FormControl<boolean>;
  sorachi_ace: FormControl<boolean>;
}
